Generated with the built-in image generation tool.

Prompt:

Use case: logo-brand. Create one simple macOS Dock app icon for Randomizer, a local random drawing app with a warm neutral and muted teal interface. Square 1024x1024 asset. One warm ivory die face with five large muted dark teal circular pips, face-on, slightly rotated, centered on a muted teal rounded-square app tile. Bold minimal geometry, flat colors, excellent legibility at 32 pixels, generous spacing. Transparent outside the rounded-square tile, tile inset about 8% from canvas edges. No text, no letters, no border, no other objects, no mockup, no background scene. Deliver the icon itself.

Source artwork: `randomizer-icon.png`. The macOS build converts it into all standard icon sizes and packages them as `applet.icns` before signing the launcher.
